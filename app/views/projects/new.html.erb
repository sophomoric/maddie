<%= simple_form_for(@project) do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>

  <%= f.input :avatar, label: "Cover Photo" %>

  <%= f.fields_for :photos do |ff| %>
    <%= ff.input :avatar %>
  <% end %>

  <%= f.submit %>
<% end %>
