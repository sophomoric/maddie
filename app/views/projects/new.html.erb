<%= simple_form_for(@project) do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :url_key %>

  <%= f.input :avatar, label: "Upload Cover Photo" %>

  <%= f.fields_for :photos do |ff| %>
    <%= ff.input :avatar %>
  <% end %>

  <%= f.submit %>
<% end %>
