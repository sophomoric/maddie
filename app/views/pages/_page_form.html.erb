<%= form_for(page) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :body %>
  <%= f.text_field :body %>

  <%= f.label :order %>
  <%= f.text_field :order %>

  <%= f.submit %>
<% end %>
