<%= simple_form_for(page) do |f| %>
  <%= f.input :title %>
  <%= f.input :body %>
  <%= f.input :order %>
  <%= f.input :url_key %>
  <%= f.submit %>
<% end %>

<div class="preview">
  <%= MarkdownHelper.new(page.body).render %>
</div>
